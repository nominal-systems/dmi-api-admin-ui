# CSS Guidelines

- Styles are built with TailwindCSS. Use utility classes and `@apply` when appropriate.
- Indent with 2 spaces and ensure files end with a newline.
- Import additional files through `index.css` using `@import`.

